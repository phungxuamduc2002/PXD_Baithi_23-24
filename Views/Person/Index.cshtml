@model IEnumerable<PXDBaiThi_23_24.Models.Person>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Msv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiaChi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Msv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiaChi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Ten">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Ten">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Ten">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
